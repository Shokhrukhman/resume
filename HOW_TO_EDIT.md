# 📝 Как редактировать резюме

## 🎯 Главный файл для правок

**Все данные резюме находятся в одном файле:**
```
content/profile.json
```

---

## 📋 Что можно редактировать

### 1️⃣ **Личная информация** (раздел `"personal"`)
- Имя, должность, локация
- Email, телефон, Telegram, WhatsApp, LinkedIn

### 2️⃣ **Описание** (поле `"summary"`)
- Ваше профессиональное описание
- Можно использовать **жирный текст** через `**текст**`

### 3️⃣ **Навыки** (раздел `"skills"`)
- Технические навыки (`"technical"`)
- Аналитические навыки (`"analytical"`)

### 4️⃣ **Опыт работы** (раздел `"experience"`)
- Добавляйте новые места работы
- Обновляйте достижения
- Меняйте периоды работы

### 5️⃣ **Образование** (раздел `"education"`)
- Обновляйте информацию об образовании

### 6️⃣ **Достижения** (раздел `"achievements"`)
- Добавляйте новые достижения

### 7️⃣ **Проекты** (раздел `"projects"`)
- Добавляйте новые проекты
- Обновляйте описания

---

## 🚀 Как внести изменения

### Вариант 1: Через VS Code (или другой редактор)
1. Откройте файл `content/profile.json`
2. Внесите изменения
3. Сохраните файл
4. Выполните команды:
   ```bash
   git add content/profile.json
   git commit -m "Обновил резюме: [опишите что изменили]"
   git push
   ```

### Вариант 2: Через GitHub веб-интерфейс
1. Откройте https://github.com/Shokhrukhman/resume
2. Найдите файл `content/profile.json`
3. Нажмите кнопку **"Edit"** (карандаш)
4. Внесите изменения
5. Прокрутите вниз, напишите сообщение коммита
6. Нажмите **"Commit changes"**
7. Сайт обновится автоматически (если на Vercel)

---

## ⚠️ Важно при редактировании JSON

1. **Всегда используйте запятые** между элементами
2. **Закрывайте кавычки** для всех строк
3. **Проверяйте скобки** `{ }` и `[ ]` — они должны быть парными
4. **Не используйте запятые** после последнего элемента

**Проверка JSON:**
```bash
npm run validate
```

Или онлайн: https://jsonlint.com/

---

## 📝 Примеры редактирования

### Добавить новое место работы:
```json
{
  "experience": [
    {
      "company": "Новая Компания",
      "position": "Новая Должность",
      "period": "2025 — Present",
      "achievements": [
        "Достижение 1",
        "Достижение 2"
      ]
    },
    // ... остальные места работы
  ]
}
```

### Добавить новый навык:
```json
{
  "skills": {
    "technical": [
      "Python",
      "Новый навык"  // ← просто добавьте
    ]
  }
}
```

### Изменить описание:
```json
{
  "summary": "Ваше новое описание. Можно использовать **жирный текст**."
}
```

---

## 🔄 После изменений

1. **Проверьте JSON**: `npm run validate`
2. **Проверьте локально**: `npm run dev`
3. **Закоммитьте**: `git commit -m "Описание изменений"`
4. **Запушьте**: `git push`
5. **Сайт обновится** автоматически (если на Vercel)

---

## 💡 Полезные команды

```bash
# Проверить JSON
npm run validate

# Запустить локально для проверки
npm run dev

# Посмотреть что изменилось
git status
git diff content/profile.json
```

---

**Готово! Теперь вы знаете, где и как редактировать резюме! 🚀**
