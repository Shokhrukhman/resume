# 📝 Быстрая шпаргалка по редактированию резюме

## 🎯 Главное правило

**Все изменения делаются в одном файле**: `content/profile.json`

После изменений:
1. Сохраните файл
2. Закоммитьте в Git: `git commit -m "Обновил резюме"`
3. Запушьте: `git push`
4. Сайт обновится автоматически (если на Vercel)

---

## 📋 Шаблоны для копирования

### ➕ Добавить новое место работы

```json
{
  "company": "Название компании",
  "position": "Должность",
  "period": "2025–2026",
  "achievements": [
    "Достижение 1",
    "Достижение 2",
    "Достижение 3"
  ]
}
```

**Куда вставить**: В массив `"experience"` (можно в начало для новых мест)

---

### ➕ Добавить новый навык

**Технический навык:**
```json
"Python (automation & analytics)"
```

**Аналитический навык:**
```json
"Data Analysis"
```

**Куда вставить**: 
- Технические → `"skills"."technical"` (массив)
- Аналитические → `"skills"."analytical"` (массив)

---

### ➕ Добавить новый проект

```json
{
  "title": "Название проекта",
  "description": "Подробное описание проекта и что было сделано",
  "technologies": ["Python", "PostgreSQL", "n8n"],
  "impact": "Измеримый результат (например: сократил время на 50%)"
}
```

**Куда вставить**: В массив `"projects"`

---

### ➕ Добавить достижение

```json
"Ваше новое достижение"
```

**Куда вставить**: В массив `"achievements"`

---

### ➕ Добавить образование

```json
{
  "degree": "Название степени",
  "institution": "Название учебного заведения",
  "period": "2020–2024"
}
```

**Куда вставить**: В массив `"education"`

---

## 🔄 Обновить существующее

### 📧 Изменить email/телефон/контакты

Найдите раздел `"personal"` и измените нужное поле:

```json
{
  "personal": {
    "email": "новый@email.com",
    "phone": "+7 (999) 123-45-67",
    "telegram": "https://t.me/новый_username",
    "whatsapp": "https://wa.me/79991234567",
    "linkedin": "https://www.linkedin.com/in/новый-профиль/"
  }
}
```

### 📝 Изменить описание (Summary)

Найдите поле `"summary"` в начале файла и замените текст.

---

## ⚠️ Частые ошибки

### ❌ Лишняя запятая
```json
["Python", "PostgreSQL",]  // ← НЕПРАВИЛЬНО
```

### ✅ Правильно
```json
["Python", "PostgreSQL"]  // ← ПРАВИЛЬНО
```

### ❌ Незакрытые кавычки
```json
"company": "Название компании  // ← НЕПРАВИЛЬНО (нет закрывающей кавычки)
```

### ✅ Правильно
```json
"company": "Название компании"  // ← ПРАВИЛЬНО
```

---

## 🧪 Проверка перед сохранением

1. **Валидация JSON**: https://jsonlint.com/
2. **Локальный тест**: `npm run dev` → откройте http://localhost:3000
3. **Проверьте консоль браузера** (F12) на ошибки

---

## 💡 Полезные советы

- ✅ **Сохраняйте копию** `profile.json` перед большими изменениями
- ✅ **Один коммит = одно изменение** (легче откатывать)
- ✅ **Используйте форматирование** — читаемый JSON легче редактировать
- ✅ **Тестируйте локально** перед пушем в продакшн

---

## 🚨 Если что-то сломалось

1. **Откатите изменения:**
   ```bash
   git checkout content/profile.json
   ```

2. **Или восстановите из бэкапа** (если делали копию)

3. **Проверьте JSON валидность** на jsonlint.com

---

**Готово! Теперь вы можете легко обновлять своё резюме! 🚀**
