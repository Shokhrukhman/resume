# 🚀 Инструкция по выгрузке на GitHub и деплою

## Шаг 1: Получите URL вашего репозитория

1. Откройте ваш репозиторий на GitHub
2. Нажмите кнопку **"Code"** (зелёная кнопка)
3. Скопируйте URL (HTTPS или SSH)
   - Пример HTTPS: `https://github.com/ваш-username/название-репозитория.git`
   - Пример SSH: `git@github.com:ваш-username/название-репозитория.git`

---

## Шаг 2: Выполните команды в терминале

Откройте терминал в папке проекта и выполните:

```bash
# 1. Добавить все файлы в Git
git add .

# 2. Сделать первый коммит
git commit -m "Initial commit: Resume website"

# 3. Переименовать ветку в main (если нужно)
git branch -M main

# 4. Добавить ваш GitHub репозиторий (ЗАМЕНИТЕ URL на ваш!)
git remote add origin https://github.com/ваш-username/название-репозитория.git

# 5. Загрузить код на GitHub
git push -u origin main
```

---

## Шаг 3: Выберите способ деплоя

### 🚀 Вариант A: Vercel (РЕКОМЕНДУЕТСЯ - проще и быстрее)

Vercel автоматически поддерживает Next.js, не нужно ничего настраивать.

1. Откройте https://vercel.com
2. Войдите через GitHub аккаунт
3. Нажмите **"Add New Project"**
4. Выберите ваш репозиторий `Shokhrukhman/resume`
5. Нажмите **"Import"**
6. Vercel автоматически определит Next.js
7. Нажмите **"Deploy"**
8. Готово! Сайт будет доступен через 1-2 минуты

**Преимущества:**
- ✅ Автоматический деплой при каждом push
- ✅ Поддержка всех функций Next.js
- ✅ Быстрая работа
- ✅ Бесплатный план

---

### 📄 Вариант B: GitHub Pages (если нужен именно GitHub)

**Важно:** GitHub Pages работает только со статическими файлами. Next.js будет экспортирован в статику.

#### Настройка GitHub Pages:

1. **Включите GitHub Pages в настройках репозитория:**
   - Откройте https://github.com/Shokhrukhman/resume/settings/pages
   - В разделе "Source" выберите **"GitHub Actions"**
   - Сохраните

2. **Загрузите код на GitHub** (если еще не загрузили):
   ```bash
   git add .
   git commit -m "Configure for GitHub Pages"
   git push -u origin main
   ```

3. **GitHub Actions автоматически соберёт и задеплоит сайт:**
   - Откройте вкладку **"Actions"** в вашем репозитории
   - Дождитесь завершения workflow "Deploy to GitHub Pages"
   - Сайт будет доступен по адресу: `https://shokhrukhman.github.io/resume/`

**Ограничения GitHub Pages:**
- ⚠️ Некоторые функции Next.js могут не работать (API routes, серверные компоненты)
- ⚠️ Медленнее, чем Vercel
- ⚠️ Нужна настройка базового пути, если репозиторий не в корне

---

### 🔄 Вариант C: Netlify (альтернатива Vercel)

1. Откройте https://netlify.com
2. Войдите через GitHub
3. Нажмите **"Add new site" → "Import an existing project"**
4. Выберите репозиторий
5. Настройки: Build command: `npm run build`, Publish directory: `.next`
6. Нажмите **"Deploy site"**

### Вариант A: Через веб-интерфейс Vercel

1. Откройте https://vercel.com
2. Войдите через GitHub аккаунт
3. Нажмите **"Add New Project"**
4. Выберите ваш репозиторий
5. Нажмите **"Import"**
6. Vercel автоматически определит Next.js
7. Нажмите **"Deploy"**
8. Готово! Сайт будет доступен через 1-2 минуты

### Вариант B: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# В папке проекта выполните
vercel

# Следуйте инструкциям в терминале
```

---

## ✅ После деплоя

- Сайт будет доступен по адресу: `https://ваш-проект.vercel.app`
- При каждом `git push` сайт будет автоматически обновляться
- Для обновления резюме просто редактируйте `content/profile.json` и пушите изменения

---

## 🔧 Если что-то пошло не так

### Ошибка "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/ваш-username/название-репозитория.git
```

### Ошибка при push
```bash
# Если репозиторий не пустой на GitHub
git pull origin main --allow-unrelated-histories
git push -u origin main
```

### Проверить текущий remote
```bash
git remote -v
```

---

## 📝 Быстрые команды для будущих обновлений

```bash
# После редактирования content/profile.json
git add content/profile.json
git commit -m "Обновил резюме"
git push
# Сайт обновится автоматически через 30-60 секунд
```
